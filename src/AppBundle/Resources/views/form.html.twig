{{ form_start(form) }}
{{ form_widget(form) }}
    {# form_widget(form.name) #}
    {# form_widget(form.color) #}
    {# form_widget(form.age) #}
    {# form_widget(form.master) #}
    {# form_widget(form.mouseKilled) #}
    {# form_widget(form.date) #}
{{ form_end(form) }}